<div class="userContainer" id = {{this.id}}>
		<form  method="POST" action="/add-friend/{{id}}" class="navbar-form navbar-left results" role="search">
			<img class="usrPic" src="{{this.ImageLink}}">
			<p class="friendName">{{this.UserName}}</p>
			<button type="submit" class="btn btn-default getFriendsButton" id={{id}}>Add Friend</button>
		</form>
	

</div>